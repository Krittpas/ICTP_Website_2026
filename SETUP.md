# ตั้งค่าก่อนใช้งานจริง

## 1. สร้างโปรเจกต์ Supabase แล้วรัน migration

SQL Editor → รันเรียงตามลำดับ

| ไฟล์ | ทำอะไร |
|---|---|
| `001_schema.sql` | ตารางทั้งหมด |
| `002_functions.sql` | ตรรกะลูกโซ่ · กฎอีเมล · คำสั่งพี่ค่าย |
| `003_rls.sql` | RLS + สิทธิ์ระดับคอลัมน์ |
| `004_realtime.sql` | กระจายความคืบหน้าแบบเรียลไทม์ |
| `005_seed.sql` | 6 เมือง + ปริศนาตัวอย่าง 36 ข้อ |
| `006_fixes.sql` | แก้โซ่ค้างเมื่อปลดที่นั่งข้ามลำดับ · สถานะล็อกกลับ · นับจำนวนปริศนาจริง |
| `007_fixes.sql` | ปุ่มสุ่มจัดเมืองใช้ได้ผ่านเว็บ · จัดเมืองซ้ำได้ · ไม่ส่งโจทย์ก่อนค่ายเปิด |
| `008_fixes.sql` | จำกัดการเดาด่านสุดท้าย · ปิดฟังก์ชันภายในจากภายนอก · กระดานเมืองดูได้เฉพาะเมืองตัวเอง · ตั้งด่านสุดท้ายผ่านเว็บ |
| `009_fixes.sql` | แถบรายเมืองแสดงที่นั่งที่ปลดข้ามลำดับถูกต้อง · ปริศนาข้อใหม่ต้องมีเฉลย · เพิ่มปริศนาแล้วโซ่คำนวณใหม่ |
| `010_senior_reveal.sql` | เครื่องถอดรหัสแบบรายคน: น้องใส่รหัสลับของตัวเอง แล้วเห็นพี่รหัสของตัวเอง · ตารางจับคู่พี่รหัส |
| `011_announcement_attachments.sql` | ประกาศแนบรูป/ไฟล์ได้ (สร้างที่เก็บไฟล์ `announcements` ให้เอง) · ช่อง "ประกาศจาก" |
| `012_puzzle_images.sql` | ปริศนาเป็นรูปภาพ + คำใบ้ใต้รูป (ที่เก็บ `puzzles` แบบส่วนตัว) · ดึงปริศนาเดิมมาแก้ต่อได้ |
| `013_profile_avatars.sql` | ทุกคนเปลี่ยนรูปโปรไฟล์ของตัวเองได้ (ที่เก็บ `avatars`) |
| `014_student_names.sql` | ชื่อ-นามสกุลและชื่อเล่นภาษาไทย ตั้งโดยพี่ค่ายเท่านั้น · น้องแก้ได้แค่รูป |
| `015_password_reset.sql` | พี่ค่ายออกรหัสผ่านใหม่ให้น้องที่ลืมรหัสได้จากหน้า `/admin` |
| `016_seniors_and_codes.sql` | พี่รหัสเป็นตารางของตัวเอง คำใบ้ผูกกับพี่ไม่ใช่น้อง · รหัสลับประจำตัวระบบสุ่มให้ 18 อักขระ |
| `017_camp_ops.sql` | ปิด/เปิดที่นั่ง · ย้ายน้องรายคนได้ตลอดเวลา · ตรวจความพร้อมก่อนวันงาน · สถานะรายเมืองหน้างาน |

> ถ้าเคยรันไฟล์ก่อนหน้าไปแล้ว ให้รันเฉพาะไฟล์ที่ยังไม่ได้รัน เช่นเคยรันถึง 016 ก็รัน `017_camp_ops.sql` ไฟล์เดียว
>
> `016` ย้ายข้อมูลพี่รหัสที่ตั้งไว้แล้วให้เอง — ชื่อพี่ที่ซ้ำกันหลายแถวถูกยุบเหลือคนเดียว
> และคำใบ้ของแถวที่แก้ล่าสุดถูกยกไปเป็นคำใบ้ของพี่คนนั้น รันซ้ำได้โดยไม่พัง

หรือถ้ามี Supabase CLI: `supabase db push`

> ทดสอบแล้วบน PostgreSQL 16 โดยรันทั้งชุดบนฐานข้อมูลเปล่า

## 2. ใส่อีเมลพี่ค่าย

```sql
INSERT INTO public.admin_emails (email, note) VALUES
  ('xxxxx@bj.ac.th', 'พี่หนึ่ง');
```

ใครอยู่ในตารางนี้จะได้ `role = 'admin'` อัตโนมัติตอนสร้างบัญชี
คนอื่นต้องเป็นอีเมลรูปแบบ `sXXXXX@bj.ac.th` เท่านั้น นอกนั้นสร้างบัญชีไม่ได้เลย

## 3. ปิดการสมัครด้วยตัวเอง

Authentication → Providers → Email → ปิด **Enable Sign Ups**

## 4. ตั้งค่า environment

```
NEXT_PUBLIC_SUPABASE_URL=...
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=...
SUPABASE_SERVICE_ROLE_KEY=...        # ห้ามหลุดไปฝั่งเบราว์เซอร์
```

## 5. สร้างบัญชีน้องค่าย

วิธีที่เร็วที่สุดคือหน้า `/admin` → **สร้างบัญชีทีละหลายคน**
เตรียมใน Google Sheets 3 คอลัมน์ (อีเมลน้อง · ชื่อ-นามสกุล · ชื่อเล่น) คัดลอกมาวางแล้วกดสร้าง
เสร็จแล้ว **ดาวน์โหลดไฟล์ CSV** ที่มีรหัสผ่านของทุกคนเก็บไว้ทันที รหัสผ่านแสดงครั้งเดียว

> ระบบส่งทีละ 5 คน ระหว่างสร้างอย่าปิดหน้า · แถวที่มีบัญชีอยู่แล้วจะข้ามไปโดยไม่กระทบแถวอื่น
> ไฟล์ CSV มีรหัสผ่านแบบอ่านได้ เก็บให้ดีและลบทิ้งเมื่อแจกน้องครบแล้ว

สร้างทีละคนก็ได้ที่ส่วน **สร้างบัญชีน้องค่าย** หรือจาก Supabase dashboard (Authentication → Add user)
ทั้งสองทางระบบจะคืนรหัสผ่านให้ครั้งเดียว ต้องจดไปแจก

น้องลืมรหัสผ่าน = กดไอคอนกุญแจท้ายแถวของน้องในส่วน **รายชื่อน้องค่าย** ระบบจะสุ่มรหัสใหม่ให้ แสดงครั้งเดียวเช่นกัน
(รหัสเดิมดูย้อนหลังไม่ได้ ฐานข้อมูลเก็บแบบถอดกลับไม่ได้)

ชื่อ-นามสกุลและชื่อเล่นต้องเป็นภาษาไทย น้องแก้เองไม่ได้ ถ้าสร้างจาก Supabase dashboard ชื่อจะเป็นรหัสนักเรียน
ให้แก้ที่ส่วน **รายชื่อน้องค่าย** ในหน้า `/admin` (คนที่ยังต้องแก้จะมีป้าย "ต้องแก้") หรือวางจาก Google Sheets ทีเดียว

## 6. จัดเมืองและใส่โจทย์

เข้า `/admin` ด้วยบัญชีพี่ค่าย แล้ว

1. กด **สุ่มจัดน้องลง 6 เมือง** (seed เดิมให้ผลเดิมเสมอ)
2. ใส่โจทย์จริงทีละข้อในส่วน **คลังปริศนา** — 36 ข้อ เลือกเมืองกับคาวบอย
   โจทย์ใส่ได้ทั้ง **รูปภาพ** และ **ข้อความ** และมีช่อง **คำใบ้** ที่แสดงใต้รูป (ทั้งสามช่องเว้นว่างได้ ขอแค่มีอย่างน้อยหนึ่งอย่าง)
   ส่วน **รหัสลับที่จะได้รับ** ไม่ต้องคิดเอง — ระบบสุ่มให้ 18 อักขระตอนกดบันทึก
3. ตั้งพี่รหัสในส่วน **จับคู่พี่รหัส** ซึ่งแบ่งเป็นสองขั้น
   - **ขั้นที่ 1 ทะเบียนพี่รหัส** — ใส่ชื่อ ชื่อเล่น และ **คำใบ้** ของพี่แต่ละคน
     คำใบ้ผูกกับพี่ ไม่ใช่กับน้อง แก้ครั้งเดียวน้องทุกคนของพี่คนนั้นเห็นตรงกัน
   - **ขั้นที่ 2 จับคู่น้องเข้ากับพี่** — เลือกน้องแล้วเลือกพี่จากรายการ
     หรือวางจาก Google Sheets ทีเดียว (คอลัมน์ อีเมลน้อง | ชื่อพี่ | ชื่อเล่นพี่ | คำใบ้)
     พี่ที่ยังไม่มีในทะเบียนจะถูกเพิ่มให้เอง · ตารางด้านล่างบอกว่าใครยังไม่ได้จับคู่
4. ตั้งเวลาเปิด แล้วกด **เปิดระบบ** — ถึงเวลาแล้วระบบไม่เปิดเอง เวลานี้ใช้แค่กับนาฬิกานับถอยหลัง ต้องกดเปิดเองเสมอ

> ปริศนาตัวอย่างใน `005_seed.sql` ตอบว่า `ictp-<เมือง>-<ที่นั่ง>`
> เช่นเมือง 4 ที่นั่ง 3 ตอบ `ictp-4-3` — ใช้ทดสอบว่าระบบเดินครบวงก่อนใส่โจทย์จริง

### รหัสลับประจำตัว

ระบบสุ่มให้ **18 อักขระ** ครบทั้งพิมพ์ใหญ่ พิมพ์เล็ก ตัวเลข และอักขระพิเศษ
ตัดตัวที่อ่านสับสนออกหมด (`0 O o` และ `1 l I`) เพราะน้องต้องอ่านจากหน้าจอแล้วพิมพ์ตาม
ตอนใส่ในเครื่องถอดรหัสระบบ**ไม่สนตัวพิมพ์เล็กใหญ่** จึงไม่ต้องลุ้นเรื่องแคปส์ล็อก

- รหัสของข้อไหนดูได้จากส่วน **คลังปริศนา** เลือกเมืองกับคาวบอยแล้วกด **คัดลอก**
- อยากเปลี่ยนรหัสข้อเดียว กด **สุ่มรหัสใหม่** แล้วบันทึก
- ตั้งค่าครั้งแรกให้กด **สุ่มรหัสลับใหม่ทุกข้อ** ท้ายส่วนคลังปริศนา เพื่อทิ้งรหัสตัวอย่างที่มากับ seed
  ข้อที่มีคนไขผ่านไปแล้วจะถูกข้ามเสมอ เพราะน้องจดรหัสเดิมไปใช้แล้ว

## 7. ก่อนกดเปิดระบบ — กดตรวจความพร้อมก่อนหนึ่งครั้ง

บนสุดของหน้า `/admin` มีส่วน **ตรวจความพร้อมก่อนวันงาน** รวมทุกอย่างที่ต้องครบไว้ในที่เดียว
กด **ตรวจอีกครั้ง** ได้ตลอด ข้อที่ขึ้น**สีแดง**คือระบบจะเดินไม่ได้จริงถ้าไม่แก้ สีเหลืองคือควรแก้แต่ยังเล่นได้

| ข้อตรวจ | ทำไมถึงสำคัญ |
|---|---|
| ที่นั่งที่เปิดอยู่แต่ไม่มีใครนั่ง | โซ่ทั้งเมืองจะค้างตรงนั้นถาวร — ย้ายน้องมาลง หรือปิดที่นั่ง |
| ปริศนาที่ไม่มีทั้งรูปและข้อความโจทย์ | น้องเปิดมาเจอแค่ชื่อด่าน |
| น้องที่ยังไม่มีที่นั่ง | ไม่มีปริศนาให้ทำเลย |
| น้องที่ยังไม่ได้จับคู่พี่รหัส | ถอดรหัสถูกแล้วแต่ไม่มีอะไรให้ดู |
| ชื่อที่ยังไม่ใช่ภาษาไทย · รหัสลับที่ยังเป็นของ seed · พี่รหัสที่ยังไม่มีคำใบ้ · อีเมลที่พิมพ์ผิด | ไม่ถึงกับพัง แต่หน้างานจะงง |

### ที่นั่งที่ไม่มีคนนั่ง

น้องมาไม่ครบ 36 คน หรือมีคนไม่มา ที่นั่งนั้นจะทำให้ทั้งเมืองค้าง มีสองทาง

- **ปิดที่นั่ง** — ในส่วน **คลังปริศนา** เลือกเมืองกับคาวบอยคนนั้น แล้วกด **ปิดที่นั่งนี้**
  โซ่จะข้ามไปคนถัดไปทันที และจำนวนปริศนาทั้งค่ายลดลงตามจริง (กุญแจทองคำยังครบได้)
  เปิดกลับได้ตลอด · ที่นั่งที่ไขผ่านไปแล้วปิดไม่ได้
- **ปลดที่นั่ง** — ในส่วน **ปลดที่นั่งที่ค้าง** ใช้เมื่ออยากให้ถือว่าที่นั่งนั้น "ผ่าน" ไปเลย
  (บันทึกเป็นการไขผ่านโดยพี่ค่าย) เหมาะกับกรณีน้องอยู่แต่ติดจริง ๆ ไม่ใช่กรณีไม่มีคน

### ย้ายน้องรายคน

ส่วน **ย้ายน้องรายคน** ใช้ได้ตลอดเวลา แม้ค่ายเริ่มไปแล้ว (ต่างจากปุ่มสุ่มจัดเมืองที่ใช้ได้ก่อนเริ่มเท่านั้น)
เลือกน้อง เลือกเมืองกับที่นั่งปลายทาง แล้วกดย้าย — ถ้าที่นั่งนั้นมีคนอยู่ ระบบจะ**สลับที่กันให้**
ไม่มีใครหลุดที่นั่งเงียบ ๆ · ปุ่ม **เอาออกจากที่นั่ง** ใช้เมื่อน้องไม่มา (บัญชียังอยู่ครบ)

> เอาน้องออกจากที่นั่งแล้วอย่าลืม **ปิดที่นั่ง** นั้นด้วย ไม่อย่างนั้นโซ่จะค้าง

### ระหว่างวันงาน

ส่วน **ตอนนี้ค้างที่ใคร** ใต้ส่วนตรวจความพร้อม บอกทีละเมืองว่ารอใครอยู่และค้างมานานกี่นาที
เรียงเมืองที่ค้างนานที่สุดขึ้นก่อน และอัปเดตเองเมื่อมีคนตอบถูก
แดง = ที่นั่งนั้นไม่มีคนนั่ง (ต้องจัดการทันที) · เหลือง = ค้างเกิน 20 นาที ลองเดินไปดู

## ตรวจว่าใช้ได้จริง

| ตรวจ | ผลที่ควรได้ |
|---|---|
| เข้าหน้าแรกตอนยังไม่เปิดค่าย | ขึ้น coming soon พร้อมนับถอยหลัง |
| ล็อกอินด้วยอีเมลที่ไม่ใช่ `@bj.ac.th` | เตือนโดยไม่ยิงไป Supabase |
| น้องที่นั่ง 3 เปิดหน้าปริศนาก่อนถึงคิว | ไม่เห็นข้อความโจทย์เลย |
| น้องที่นั่ง 1 เปิดหน้าปริศนาก่อนค่ายเปิด | ขึ้น "ค่ายยังไม่เปิด" ไม่เห็นโจทย์ |
| กดสุ่มจัดเมือง 2 ครั้งด้วย seed ต่างกัน (ก่อนมีคนตอบ) | สำเร็จทั้งสองครั้ง |
| ตั้งเวลาเปิด แล้วกดเปิด/ปิดระบบหลายรอบ | เวลาในช่องไม่เลื่อน |
| น้องที่นั่ง 1 ตอบถูก | ที่นั่ง 2 เปิดเองโดยไม่ต้องรีเฟรช |
| ตอบผิด 5 ครั้งใน 10 นาที | `rate_limited` |
| ตอบครบทุกที่นั่ง (กุญแจทองคำครบ 6 ชิ้น) | ประตูสำนักงานนายอำเภอเปิดเอง โหมด `auto` |
| พี่ค่ายกดเปิดประตูเอง | เปิดทันที โหมด `manual` พร้อมบันทึกเหตุผล |
| นักเรียนเรียกคำสั่งของพี่ค่าย | `forbidden` |
| นักเรียนอ่านตาราง `puzzles` ตรง ๆ | `permission denied` |
| น้องใส่รหัสลับของตัวเองในเครื่องถอดรหัส | เห็นชื่อพี่รหัสของตัวเอง กลับมาเปิดหน้าใหม่ก็ยังเห็น |
| น้องใส่รหัสลับของเพื่อน | "รหัสนี้ไม่ใช่ของคุณ" |
| ใส่รหัสผิด 5 ครั้งใน 10 นาที | `rate_limited` |
| รหัสถูกแต่ยังไม่ได้จับคู่พี่รหัสให้ | "พี่ค่ายยังไม่ได้บันทึกพี่รหัสของคุณ" |
| บันทึกปริศนาข้อใหม่โดยไม่ได้กรอกรหัสลับ | ได้รหัสสุ่ม 18 อักขระ มีทั้งพิมพ์ใหญ่ พิมพ์เล็ก ตัวเลข อักขระพิเศษ |
| กด **สุ่มรหัสใหม่** ที่ข้อเดิมแล้วบันทึก | ได้รหัสชุดใหม่ · รหัสเดิมใช้กับเครื่องถอดรหัสไม่ได้อีก |
| กด **สุ่มรหัสลับใหม่ทุกข้อ** ตอนมีคนไขผ่านไปแล้ว 5 ข้อ | 5 ข้อนั้นไม่ถูกแตะ บอกจำนวนที่ข้ามกลับมา |
| น้องใส่รหัสของตัวเองโดยพิมพ์เป็นตัวเล็กหมด | ผ่าน — เครื่องไม่สนตัวพิมพ์เล็กใหญ่ |
| แก้คำใบ้ของพี่หนึ่งคนที่มีน้อง 3 คน | น้องทั้ง 3 เห็นคำใบ้ใหม่ตรงกัน ไม่ต้องแก้ทีละคน |
| ลบพี่รหัสที่ยังมีน้องผูกอยู่ | ถูกปฏิเสธ พร้อมบอกว่ามีน้องกี่คน |
| วางสเปรดชีตที่มีชื่อพี่คนเดิมซ้ำหลายบรรทัด | ได้พี่คนเดียวในทะเบียน น้องทุกคนผูกกับพี่คนนั้น |
| นักเรียนอ่านตาราง `seniors` ตรง ๆ | `permission denied` |
| พี่ค่ายติดประกาศพร้อมรูป 2 รูปและ PDF | รูปเรียงเป็นตาราง PDF กดดาวน์โหลดได้ชื่อไฟล์เดิม |
| แก้ประกาศแล้วเอารูปออก | รูปหายจากประกาศ และถูกลบจาก Storage ด้วย |
| น้องอัปโหลดไฟล์เข้าที่เก็บ `announcements` ตรง ๆ | ถูกปฏิเสธ (มีแต่พี่ค่ายที่อัปโหลดได้) |
| คาวบอยที่ถึงตาเปิดหน้าปริศนา | เห็นรูปโจทย์ และคำใบ้ใต้รูป |
| คาวบอย #3 ขอลิงก์รูปโจทย์ของตัวเองก่อนถึงตา | ถูกปฏิเสธ — policy ของ Storage ไม่ออกลิงก์ให้ |
| พี่ค่ายเลือกที่นั่งที่มีปริศนาแล้วในคลังปริศนา | ฟอร์มเติมชื่อด่าน รูป คำใบ้ รหัสลับเดิมให้ (เฉลยไม่แสดง) |
| น้องเปลี่ยนรูปโปรไฟล์ | รูปขึ้นในเมนูโปรไฟล์ทันที รูปเก่าถูกลบจาก Storage |
| น้องแก้ `display_name` หรือ `nickname` ของตัวเองผ่าน API | `permission denied` |
| พี่ค่ายตั้งชื่อน้องเป็นภาษาอังกฤษ หรือไม่มีนามสกุล | ถูกปฏิเสธ พร้อมบอกบรรทัดที่ผิด |
| พี่ค่ายกดออกรหัสผ่านใหม่ให้น้อง | ได้รหัสใหม่มาจด · น้องเข้าด้วยรหัสเดิมไม่ได้แล้ว · มีบันทึกใน `admin_audit` |
| น้องตั้ง `avatar_url` เป็นรูปในโฟลเดอร์ของคนอื่นผ่าน API | ถูกปฏิเสธด้วย `profiles_avatar_own_folder` |
| เรียก `camp_email_role` ผ่าน API | `permission denied` |
| นักเรียนเรียก `get_city_board` ของเมืองอื่น | ได้ผลว่าง |
| ปลดที่นั่ง 5 ตอนโซ่อยู่ที่ 2 | แถบรายเมืองขึ้นที่นั่ง 5 เป็นผ่านแล้ว |
| เพิ่มปริศนาที่นั่งใหม่โดยเว้นช่องเฉลย | "ปริศนาข้อใหม่ต้องใส่เฉลย" |
| ปิดที่นั่ง 3 ตอนโซ่ค้างอยู่ที่ 3 | โซ่ขยับไปที่นั่ง 4 ทันที · จำนวนปริศนาทั้งค่ายลดลง 1 |
| ปิดที่นั่งที่ไขผ่านไปแล้ว | ถูกปฏิเสธ |
| ย้ายน้องไปที่นั่งที่มีคนอยู่ | สองคนสลับที่กัน ไม่มีใครหลุดที่นั่ง |
| ตรวจความพร้อมตอนที่นั่งยังไม่มีคนนั่ง | ขึ้นแดง พร้อมบอกว่าเมืองไหนที่นั่งไหน |

## ความเสี่ยงที่ควรรู้ก่อนวันงาน

- **คนเดียวค้างทั้งเมือง** — โซ่ลูกโซ่แปลว่าถ้าคนที่ 3 ไม่มา อีก 3 คนหลังทำอะไรไม่ได้
  ทางแก้มีสามทางในหน้า `/admin`: **ปิดที่นั่ง** (ไม่มีคนนั่ง) · **ย้ายน้องรายคน** (มีคนมาแทน)
  · **ปลดที่นั่งที่ค้าง** (มีคนแต่ติดจริง ๆ) · ส่วน **ตอนนี้ค้างที่ใคร** บอกว่าเมืองไหนต้องไปดูก่อน
- **เฉลยอ่านกลับไม่ได้** — ถูกแฮชทันทีที่บันทึก ถ้าลืมต้องตั้งใหม่
  เก็บต้นฉบับไว้นอกระบบด้วย
- **จัดเมืองใหม่ยกค่ายไม่ได้หลังเริ่มเล่น** — ระบบปฏิเสธเองถ้ามีคนตอบถูกไปแล้ว
  แต่ย้ายทีละคนได้ตลอดที่ส่วน **ย้ายน้องรายคน**
- **สุ่มรหัสลับใหม่ = รหัสที่น้องจดไว้ใช้ไม่ได้ทันที** — ปุ่มสุ่มทุกข้อข้ามข้อที่ไขผ่านแล้วให้เอง
  แต่ปุ่ม **สุ่มรหัสใหม่** ของข้อเดี่ยวไม่ข้ามให้ (ฟอร์มขึ้นคำเตือนถ้าข้อนั้นมีคนไขผ่านแล้ว)
  ตั้งใจสุ่มใหม่หลังน้องจดไปแล้ว ต้องแจ้งรหัสใหม่ให้น้องเอง
- **ลบพี่รหัสที่ยังมีน้องผูกอยู่ไม่ได้** — ต้องย้ายน้องไปพี่คนอื่น หรือกด **เอาออก** ที่แถวของน้องก่อน
  เป็นการกันไม่ให้น้องหลุดคู่แบบเงียบ ๆ แล้วมารู้ตอนหน้างานว่าเครื่องถอดรหัสไม่มีอะไรให้
